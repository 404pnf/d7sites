<?php

/*
 * You must implement hook_migrate_api(), setting the API level to 2, for
 * your migration classes to be recognized by the Migrate module. 中文
 */
function migrate_fltrp_migrate_api() {
  $api = array(
    'api' => 2,
  );
  return $api;
}